apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: CHANGEME
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: CHANGEME
  filterTags:
    pattern: '^[0-9]+$'
  policy:
    numerical:
      order: asc
